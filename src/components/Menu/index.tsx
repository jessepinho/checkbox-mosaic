import React from 'react'

import './styles.css'

const Menu: React.FC<{}> = props => (
  <nav className="Menu">
    <button className="Menu__toggle-button">Toggle menu</button>
  </nav>
)

export default Menu
